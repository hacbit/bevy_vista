# bevy_vista_macros

Procedural macros used by `bevy_vista`.

## Macros

- `#[derive(Widget)]`
- `#[derive(ShowInInspector)]`
- `generate_icons!`

This crate is intended to be consumed through `bevy_vista`.
